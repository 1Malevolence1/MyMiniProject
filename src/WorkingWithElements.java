import java.util.HashMap;

public interface WorkingWithElements {
    public int randomKey(HashMap<Integer, String> randomKey);
    public String returnKeyWord(int key, HashMap<Integer, String> dictionary);
}
